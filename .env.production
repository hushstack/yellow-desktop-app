# Non-secret, build-time renderer configuration. Inlined into the bundle by Vite.
# The API base URL is a main-process setting (YELLO_API_BASE_URL), not a VITE_* one.
VITE_APP_ENV=production
VITE_ENABLE_DEVTOOLS=false
